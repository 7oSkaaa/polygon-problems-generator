% Final formatted statement — ready for Polygon
% Sections: Legend, Input, [Interaction — interactive problems only], Output, Notes

% ─── Title ───────────────────────────────────────────────────────────────────

\textbf{\Large <Problem Title>}

% ─── Legend ──────────────────────────────────────────────────────────────────


% ─── Input ───────────────────────────────────────────────────────────────────

% The first line contains a single integer $T$ ($1 \leq T \leq 10^4$) --- the number of test cases.
% Each test case consists of ...


% ─── Interaction (REMOVE THIS SECTION for non-interactive problems) ───────────
%
% In each step, read one line from the judge, then output one line and flush.
%
% \textbf{After printing each line, flush the output.} For example:
% \begin{itemize}
%     \item \texttt{cout << endl} or \texttt{fflush(stdout)} in C++;
%     \item \texttt{System.out.flush()} in Java;
%     \item \texttt{sys.stdout.flush()} in Python.
% \end{itemize}


% ─── Output ──────────────────────────────────────────────────────────────────

% Print ...


% ─── Notes ───────────────────────────────────────────────────────────────────
