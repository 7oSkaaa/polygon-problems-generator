% Tutorial — solution explanation, renderable to PDF on Polygon
% Follow the same LaTeX rules as statement.tex:
%   - All variables in math mode ($n$, $a_i$, etc.)
%   - \leq / \geq / \neq — never <=, >=, !=
%   - \times for multiplication, \ldots for sequences
%   - No \begin{document} wrapper

% ─── Key Observations ────────────────────────────────────────────────────────


% ─── Solution ────────────────────────────────────────────────────────────────


% ─── Complexity ──────────────────────────────────────────────────────────────

% Time: $O(?)$ per test case.
% Space: $O(?)$.
